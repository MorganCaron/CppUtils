export module CppUtils.UnitTests.Terminal.Title;

import std;
import CppUtils;

namespace CppUtils::UnitTest::Terminal::Title
{
	auto _ = TestSuite{"Terminal", {"UnitTest"}, [](auto& suite) {
		// using namespace std::literals;
		// using namespace std::chrono_literals;

		suite.addTest("Title", [&] {
			/*
			auto oldTitle = CppUtils::Terminal::getTerminalTitle();
			{
				auto newTitle = "Terminal Title"sv;
				auto _ = CppUtils::Terminal::Title{newTitle};
				suite.expectEqual(CppUtils::Terminal::getTerminalTitle(), newTitle);
				std::this_thread::sleep_for(3s);
			}
			suite.expectEqual(CppUtils::Terminal::getTerminalTitle(), oldTitle);
			*/
		});
	}};
}
